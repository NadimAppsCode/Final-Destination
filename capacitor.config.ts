import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'app.finalDestination.islam',
  appName: 'Final Destination',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
